services:
  trainer:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace/ai
    volumes:
      - ..:/workspace
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: python src/train_draft_value.py

  policy_trainer:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace/ai
    volumes:
      - ..:/workspace
    command: python src/train_bp_policy.py

  exporter:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace/ai
    volumes:
      - ..:/workspace
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: python src/export_onnx.py

  evaluator:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /workspace/ai
    volumes:
      - ..:/workspace
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    command: python src/evaluate.py
