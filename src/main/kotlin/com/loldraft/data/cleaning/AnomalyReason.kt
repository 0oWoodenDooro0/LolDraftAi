package com.loldraft.data.cleaning

enum class AnomalyReason {
    REMAKE,
    INSUFFICIENT_PICKS,
    DUPLICATE_CHAMPION,
    INVALID_PATCH,
    CORRUPT_TURNS,
    MISSING_TEAMS,
    VALIDATION_FAILED,
}
